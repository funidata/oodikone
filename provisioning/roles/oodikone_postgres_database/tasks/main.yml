- name: Install ansible dependencies
  import_role:
    name: funidata.infra.ansible_postgresql
  tags: ansible_postgresql

- name: Provision database user
  postgresql_user:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    name: "{{ oodikone_postgres_username }}"
    password: "{{ oodikone_postgres_password }}"
    encrypted: true
    role_attr_flags: LOGIN
    no_password_changes: "{{ not (oodikone_postgres_database_password_changes | bool) }}"
  tags: oodikone_postgres_database_users

- name: Grant role membership to postgres
  postgresql_membership:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"
    login_db: postgres

    users:
      - "{{ oodikone_postgres_admin_user }}"
    groups:
      - "{{ oodikone_postgres_username }}"
  tags: oodikone_postgres_database_users

- name: Provision database
  postgresql_db:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    name: "{{ oodikone_postgres_database }}"
    owner: "{{ oodikone_postgres_username }}"
    template: template0
    encoding: "{{ oodikone_postgres_database_encoding }}"
    lc_collate: "{{ oodikone_postgres_database_locale }}"
    lc_ctype: "{{ oodikone_postgres_database_locale }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database

# https://aws.amazon.com/premiumsupport/knowledge-center/rds-postgresql-revoke-public-schema/
- name: Alter public schema owner
  postgresql_owner:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # ALTER SCHEMA public OWNER TO ...
    db: "{{ oodikone_postgres_database }}"
    obj_type: schema
    obj_name: public
    new_owner: "{{ oodikone_postgres_username }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_public

- name: Revoke database privileges from public
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    state: absent
    database: "{{ oodikone_postgres_database }}"
    type: database
    privs: CONNECT,TEMP
    role: PUBLIC
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_public

- name: Revoke schema privileges from public
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # REVOKE CREATE, USAGE PRIVILEGES ON SCHEMA public FROM public
    state: absent
    database: "{{ oodikone_postgres_database }}"
    type: schema
    privs: CREATE,USAGE
    objs: public
    role: PUBLIC
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_public

- name: Load database extensions
  postgresql_ext:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    db: "{{ db_ext[0] }}"
    name: "{{ db_ext[1] }}"
    state: present
  loop: "{{ oodikone_postgres_databases | product(oodikone_postgres_database_extensions) | list }}"
  loop_control:
    loop_var: db_ext
  tags: oodikone_postgres_database_extensions

- name: Configure database settings
  funidata.infra.postgresql_database_setting:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    db: "{{ db_setting[0] }}"
    name: "{{ db_setting[1].key }}"
    value: "{{ db_setting[1].value }}"

  loop: "{{ oodikone_postgres_databases | product(oodikone_postgres_database_settings | dict2items) | list }}"
  loop_control:
    loop_var: db_setting
  tags: oodikone_postgres_database_settings

- name: Provision database application role settings
  funidata.infra.postgresql_role_settings:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    role: "{{ oodikone_postgres_username }}"
    settings:
      # https://github.com/pgaudit/pgaudit/blob/master/README.md#pgauditlog
      pgaudit.log: "{{ oodikone_postgres_database_user_pgaudit_log }}"
  tags: oodikone_postgres_database_user_settings

#
# read_role
#

- name: Provision database read role
  postgresql_user:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    name: "{{ oodikone_postgres_database_read_role }}"
    role_attr_flags: NOLOGIN
    no_password_changes: true # attempts to ... WITH ENCRYPTED PASSWORD NULL ... on RDS
  tags: oodikone_postgres_database_read_role

- name: Grant database read role database privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # GRANT CONNECT, TEMPORARY ON DATABASE ... TO ...
    database: "{{ oodikone_postgres_database }}"
    type: database
    privs: CONNECT,TEMPORARY
    roles: "{{ oodikone_postgres_database_read_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_read_role

- name: Provision database read role schema privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # GRANT USAGE ON SCHEMA ... TO ...
    database: "{{ oodikone_postgres_database }}"
    type: schema
    objs: public
    privs: USAGE
    roles: "{{ oodikone_postgres_database_read_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_read_role

# Use default privileges to grant access to tables added in the future
- name: Provision database read role default select privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"
    session_role: "{{ oodikone_postgres_username }}" # requires execution as superuser or altered role

    # ALTER DEFAULT PRIVILEGES FOR USER ... IN SCHEMA ... GRANT SELECT ON TABLES TO ...
    database: "{{ oodikone_postgres_database }}"
    type: default_privs
    target_roles: "{{ oodikone_postgres_username }}"
    schema: public
    privs: SELECT
    objs: TABLES,SEQUENCES
    roles: "{{ oodikone_postgres_database_read_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_read_role

- name: Provision database read role schema table select privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # GRANT SELECT ON ALL TABLES IN SCHEMA ... TO ...
    database: "{{ oodikone_postgres_database }}"
    schema: public
    type: table
    privs: SELECT
    objs: ALL_IN_SCHEMA
    roles: "{{ oodikone_postgres_database_read_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_read_role

- name: Provision database read role schema sequence select privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # GRANT SELECT ON ALL SEQUENCES IN SCHEMA ... TO ...
    database: "{{ oodikone_postgres_database }}"
    schema: public
    type: sequence
    privs: SELECT
    objs: ALL_IN_SCHEMA
    roles: "{{ oodikone_postgres_database_read_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_read_role

# Read users
- name: Grant user database read role membership
  funidata.infra.postgresql_role_members:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    role: "{{ oodikone_postgres_database_read_role }}"
    members: "{{ oodikone_postgres_read_users }}"
    strict_membership: true
  tags:
    - oodikone_postgres_database_users
    - oodikone_postgres_database_read_users

#
# admin_role
#

- name: Provision database admin role
  postgresql_user:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    name: "{{ oodikone_postgres_database_admin_role }}"
    role_attr_flags: NOLOGIN
    no_password_changes: true # attempts to ... WITH ENCRYPTED PASSWORD NULL ... on RDS
  tags: oodikone_postgres_database_admin_role

- name: Grant database admin role database privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # GRANT ALL ON DATABASE ... TO ...
    database: "{{ oodikone_postgres_database }}"
    type: database
    privs: ALL
    roles: "{{ oodikone_postgres_database_admin_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_admin_role

- name: Provision database admin role schema privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # GRANT ALL ON SCHEMA ... TO ...
    database: "{{ oodikone_postgres_database }}"
    type: schema
    objs: public
    privs: ALL
    roles: "{{ oodikone_postgres_database_admin_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_admin_role

# Use default privileges to grant access to tables added in the future
- name: Provision database admin role default privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"
    session_role: "{{ oodikone_postgres_username }}" # requires execution as superuser or altered role

    # ALTER DEFAULT PRIVILEGES FOR USER ... IN SCHEMA ... GRANT SELECT ON TABLES TO ...
    database: "{{ oodikone_postgres_database }}"
    type: default_privs
    target_roles: "{{ oodikone_postgres_username }}"
    schema: public
    privs: ALL
    objs: TABLES,SEQUENCES
    roles: "{{ oodikone_postgres_database_admin_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_admin_role

- name: Provision database admin role schema privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # GRANT SELECT ON ALL TABLES IN SCHEMA ... TO ...
    database: "{{ oodikone_postgres_database }}"
    schema: public
    type: table
    privs: ALL
    objs: ALL_IN_SCHEMA
    roles: "{{ oodikone_postgres_database_admin_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_admin_role

- name: Provision database admin role schema sequence privileges
  postgresql_privs:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    # GRANT SELECT ON ALL SEQUENCES IN SCHEMA ... TO ...
    database: "{{ oodikone_postgres_database }}"
    schema: public
    type: sequence
    privs: ALL
    objs: ALL_IN_SCHEMA
    roles: "{{ oodikone_postgres_database_admin_role }}"
  loop: "{{ oodikone_postgres_databases }}"
  loop_control:
    loop_var: oodikone_postgres_database
  tags: oodikone_postgres_database_admin_role

# Admin users
- name: Grant user database admin role membership
  funidata.infra.postgresql_role_members:
    login_host: "{{ oodikone_postgres_admin_host }}"
    login_port: "{{ oodikone_postgres_admin_port }}"
    login_user: "{{ oodikone_postgres_admin_user }}"
    login_password: "{{ oodikone_postgres_admin_password }}"

    role: "{{ oodikone_postgres_database_admin_role }}"
    members: "{{ oodikone_postgres_admin_users }}"
    strict_membership: true
  tags:
    - oodikone_postgres_database_users
    - oodikone_postgres_database_admin_users
