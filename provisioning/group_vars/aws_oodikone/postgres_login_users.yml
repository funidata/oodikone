postgres_login_users_admin_host: "{{ oodikone_postgres_admin_host }}"
postgres_login_users_admin_port: "{{ oodikone_postgres_admin_port }}"
postgres_login_users_admin_user: "{{ oodikone_postgres_admin_user }}"
postgres_login_users_admin_password: "{{ oodikone_postgres_admin_password }}"

postgres_login_users_user_role: "oodikone_user"
postgres_login_users: "{{ (oodikone_postgres_read_users + oodikone_postgres_admin_users) | unique }}"
postgres_login_users_user_pgaudit_log: ALL